# Copy to .env.local and fill in. Generate a secret with: openssl rand -base64 32
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET=

# Local Docker Postgres (docker compose up -d). Swap for Neon/Supabase URL in prod.
DATABASE_URL=postgresql://atur:atur_dev_pw@localhost:5432/atur_kelas
